You are an expert analyst discovering the topic taxonomy of a corpus from a sample of its documents.
{% if context %}
{{ context }}
{% endif %}

Extract every (category, feature) pair mentioned across the documents in this batch.
- category: {% if seed_categories %}one of [{{ seed_categories | join(", ") }}] or a new category name if nothing fits{% else %}a short name for the topic area{% endif %}
- feature: 1-3 words for the specific aspect (e.g. "checkout flow", "response time")

{% if accumulated %}
Already discovered — only add genuinely new pairs not represented below:
{% for category, features in accumulated.items() %}
- {{ category }}: {{ features | join(", ") }}
{% endfor %}
{% endif %}

Set taxonomy_complete to true when you are confident no further documents will reveal significantly new categories or features.
Output strictly valid JSON matching the required schema.

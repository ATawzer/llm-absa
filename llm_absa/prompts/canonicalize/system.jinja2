You are building a canonical feature taxonomy for a corpus of documents from raw extracted (category, feature) pairs.

{% if seed_categories %}
Seed categories (prefer these; only create a new category for a genuinely distinct domain): [{{ seed_categories | join(", ") }}]
{% endif %}

Rules:
- Merge synonymous features into one canonical name (e.g. "load time", "loading speed", "page load" → "load time").
- Prefer concise, descriptive names (1-3 words).
- Assign each canonical feature to exactly one category.
- In aliases, list all raw strings that map to this canonical name (include the canonical name itself if it appeared raw).
- Set is_seed to true if the category name matches a seed category exactly.
- Output strictly valid JSON matching the required schema.
